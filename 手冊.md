# 睇樓資料庫 · 部署手冊（v2.0）

> 本手冊供 Jay 逐步執行。全程約 30–40 分鐘，需要：Notion 帳戶（已由 Claude 建好資料庫）、AWS 帳戶（HearthStack 同一個）、GitHub 帳戶、Anthropic API key。每一步皆附「驗收標準」；未達標請停下，把錯誤訊息貼回給 Claude。

---

## 零、已由 Claude 完成（無需操作）

| 項目 | 狀態 |
|---|---|
| Notion 母頁「搬屋 2026 · 睇樓資料庫」 | ✅ https://app.notion.com/p/3d61257631e5819c80bfff99f5da96b3 |
| 六個 database：單位／房間／傢俬／代理與聯絡／出價與要求／睇樓檢查表 | ✅ 含 formula（呎租、每月總支出、入伙總成本、平均評分、評分分歧、房間呎數、檢查表問題數） |
| 傢俬 13 件 | ✅ 已匯入 |
| 單位視圖：管線看板、日曆、對比表（未淘汰） | ✅ |
| Lambda 程式內已寫死六個 database ID | ✅ 不用再填 |

---

## 一、Notion（約 3 分鐘）

**1.1 把母頁面連接 integration**
1. 開啟母頁 → 右上 `…` → **Connections**（連接）→ 搜尋 HearthStack 所用的 integration 名稱 → 加入。
2. 子頁（六個 database）會自動繼承權限。

**1.2 取得 Notion token**
- 若沿用 HearthStack 的 integration：在 HearthStack Lambda 環境變數複製 `NOTION_TOKEN`。
- 若想獨立：https://www.notion.so/my-integrations → New integration（Internal）→ 複製 secret；回到 1.1 用此新 integration。

**1.3 分享給太太**
- 母頁 → Share → 輸入太太 email → Full access。她在 Notion 只需能看；日常操作用 App。

**驗收**：母頁 Connections 內見到 integration 名稱。

---

## 二、AI key（約 1 分鐘）

沿用你現有的 Gemini key（HearthStack／KIAH Lambda 環境變數內 `GERMINI_API_KEY`）。程式會自動偵測：有 `ANTHROPIC_API_KEY` 用 Claude，否則用 Gemini。

**驗收**：`/health` 回傳 `"llm":"gemini"`。

---

## 三、AWS Lambda（約 10 分鐘）

**3.1 建立函數**
1. AWS Console → Lambda → **Create function** → Author from scratch。
2. Function name：`flat-tracker`；Runtime：**Node.js 20.x**；Architecture：arm64 或 x86_64 皆可 → Create。

**3.2 貼入程式**
1. Code 分頁 → 把左側 `index.mjs`（若預設為 `index.js`，右鍵 Rename 改為 `index.mjs`）內容全部刪除。
2. 開啟本 repo `lambda/index.mjs`，全選複製，貼入 → **Deploy**。

**3.3 設定**
1. Configuration → General configuration → Edit → **Timeout: 2 min 0 sec**；Memory 256 MB → Save。
2. Configuration → **Environment variables** → Edit → 加入：

| Key | Value | 說明 |
|---|---|---|
| `NOTION_TOKEN` | 步驟 1.2 的 secret | 必填 |
| `APP_KEY` | 自訂 20 位以上隨機字串（例：`jay-hk-2026-xK9pQ2mZ`） | 必填；前端要填相同值 |
| `GERMINI_API_KEY`（或 `GEMINI_API_KEY`） | 你現有的 Gemini key | AI 功能用；與 `ANTHROPIC_API_KEY` 二擇一，兩者皆有時優先 Claude |
| `GERMINI_MODEL`（或 `GEMINI_MODEL`） | 例：`gemini-2.5-flash` | 選填；用你截圖那個亦可，程式不驗證名稱，若 Gemini 回 404 則改此值 |
| `ANTHROPIC_API_KEY` | Anthropic key | 選填（沒有就不用填） |
| `TELEGRAM_BOT_TOKEN` | HearthStack bot token | 選填；Lambda 錯誤會推到 Telegram |
| `TELEGRAM_CHAT_ID` | 你的 chat id | 選填 |

3. Configuration → **Function URL** → Create function URL：
   - Auth type：**NONE**
   - **Configure cross-origin resource sharing (CORS)：不要勾選**（程式已處理；重複會令 Safari 拒絕）
   - Save，複製 Function URL（形如 `https://xxxx.lambda-url.ap-east-1.on.aws/`）。

**3.4 驗收（終端機或瀏覽器擴充）**
```bash
curl "https://<FUNCTION_URL>/health" -H "x-app-key: <APP_KEY>"
```
預期：
```json
{"ok":true,"notion_version":"2022-06-28","llm":"gemini","model":"gemini-2.5-flash","telegram":false,"dbs":{"flats":"0539068977b6464891ab466b2abd4b66", ...}}
```
再測 Notion 讀取：
```bash
curl "https://<FUNCTION_URL>/furniture" -H "x-app-key: <APP_KEY>"
```
預期：JSON 陣列，含 13 件傢俬。**若回 401** → APP_KEY 不符；**若回 Notion 404/403** → 步驟 1.1 未連接 integration；**若回 400 且訊息含 `data_source`** → 把錯誤貼給 Claude，改用新版 API。

---

## 四、GitHub Pages（約 5 分鐘）

**4.1 建 repo**
1. https://github.com/new → Repository name：`flat-tracker` → **Private** → Create repository。
2. 上載檔案：**Add file → Upload files** → 把本 zip 內整個 `flat-tracker` 資料夾的內容拖入（保持 `docs/index.html`、`lambda/index.mjs`、`README.md`、`手冊.md` 結構）→ Commit。

**4.2 開 Pages**
1. Settings → **Pages** → Source：Deploy from a branch → Branch：`main`，Folder：**`/docs`** → Save。
2. 約 1–2 分鐘後頁面頂部顯示網址：`https://<你的帳號>.github.io/flat-tracker/`。

> Private repo 用 GitHub Pages 需要 GitHub Pro／Team；若你是免費帳戶，改為 Public repo（程式內無任何密鑰，APP_KEY 與 URL 只存於各人手機）。

**驗收**：手機 Safari 開網址見到黃色標題「睇樓資料庫」及設定頁。

---

## 五、手機設定（每部電話各做一次，約 2 分鐘）

1. Safari 開 Pages 網址 → 分享 → **加入主畫面** → 從主畫面開啟（全螢幕，似 app）。
2. 設定頁：
   - Lambda Function URL：步驟 3.3 的 URL
   - APP KEY：與 Lambda 相同
   - 我是：**Jay** 或 **太太**（決定評分與檢查表歸屬）
   - 按「儲存並測試連線」→ 應顯示「連線成功 · Notion 2022-06-28 · AI gemini」
3. 「我們的條件」：每月預算上限、現居退租日、教會地址、工作地址 → 儲存條件。
4. 傢俬頁應見 13 件（由 Notion 拉回）。

**驗收**：新增一個測試單位 → 開啟 Notion 母頁「單位」database 見到該單位 → 在 Notion 改其階段 → 回 App 按「↻ 同步」見到更新 → 刪除測試單位。

---

## 六、日常使用流程

**睇樓前**
1. 在 28Hse／中原見到樓盤 → App「＋ 新增單位」→ 貼網址 → **抓取** → 核對自動填入的叫價／呎數／房數 → 儲存（階段：網上見到）。
2. 若同座同號單位已有紀錄，畫面會提示「同座同號」，可一鍵套用房數／呎數甚至房間尺寸。
3. 約睇後：單位頁頂部階段改「已約睇」，填睇樓日期；代理頁新增代理，並在單位「編輯」內選取。

**睇樓現場（建議分工）**
- 一人：房間分頁「＋ 新增房間」，用 iPhone 測距儀量長／闊／樓底／門闊／窗台深，即入數。
- 另一人：檢查表分頁逐項 ✓／✗／？（按一下即存）；AI 分頁「📷 相片檢查」拍疑似滲水／裂縫位置即時分析。
- 離開前：配對分頁看每間房「放不入」清單；評分（按「給 X 評分」）。

**睇樓後**
- AI 分頁「產生 AI 摘要」→ 優缺點、傢俬安置、談判籌碼、建議出價。
- 出價分頁記錄每次出價／對方還價／我方要求（拆櫃、翻新、免租期）／對方回覆，並改單位階段。
- 對比頁揀 2–4 個單位「並排對比」或「AI 比較」。
- 列表頁可按呎租、評分分歧、交樓日、教會車程排序；勾「只顯示預算內」。

**看板**
- 頂部顯示現居退租日倒數；交樓日遲於退租日的單位會警示。
- 夫妻評分分歧 ≥2 的單位標「分歧」，優先傾。

---

## 七、故障排除

| 現象 | 原因 | 處理 |
|---|---|---|
| 頂部顯示「未同步 N」 | 網絡或 Lambda 錯誤，資料已存本機 | 設定頁「重試同步」；若持續，看 Lambda CloudWatch log |
| 「抓取」回「網頁回應 403」 | 樓盤網封鎖 AWS IP | 人手填寫；或告訴 Claude 改接 Apify 28Hse actor（US$2／1,000 個） |
| AI 摘要失敗「未設定 ANTHROPIC_API_KEY 或 GEMINI_API_KEY」 | 環境變數缺 | 步驟 3.3 |
| AI 失敗「Gemini 404」 | 模型名稱不存在 | 把 `GERMINI_MODEL` 改為 `gemini-2.5-flash` |
| AI 失敗「Gemini 回傳空白」 | thinking 耗盡 token | 程式已自動關 thinking；若仍空白，改用非推理型號 |
| Safari 顯示 CORS 錯誤 | Function URL 勾了 CORS | 取消勾選 |
| 兩人同時改同一欄 | Notion 後寫者勝 | 分工：一人入數、一人填檢查表 |
| 想重設本機 | — | 設定頁「清除本機快取」，Notion 不受影響 |

---

## 八、下一階段（Phase 2，待你確認後開工）

1. **2D 平面規劃器**：以房間尺寸畫平面，拖放傢俬（比例準確）。
2. **AR 預視**：把傢俬生成 USDZ 方塊，在單位內用 iPhone AR Quick Look 投影真實尺寸。
3. **Telegram 快速輸入**：群組貼「3座12A 主人房 320×280 門75」即入 Notion。
4. **相片存 Notion**：由 App 直接上載。
5. **每晚摘要**：待跟進代理、交樓日倒數、評分分歧推送 Telegram。
